import { GetStaticProps } from "next";

interface Event{
    year: number;
    description: string;
    date: string;
    code: string;
    location: string;
}

interface Race{

}

interface Heat{
    
}